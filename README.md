# prisma graphql with pipelinedb demo

include prisma graphql && kafka && pipelinedb

## Run

* prisma graphql

```code
docker-compose up -d
```

* deploy prisma service

```code
prisma deploy
```

## notes

you may first connenct to pipeliendb with pgclient && create prisma && postgres databases
